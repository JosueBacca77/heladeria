const defaulState = 0

export default function cash(state = defaulState, {type, payload}){
    switch (type) {
        // case 'BUY_CHOCOLATE':
        //     //
        //     break;
        default:
            return state
    }
}
